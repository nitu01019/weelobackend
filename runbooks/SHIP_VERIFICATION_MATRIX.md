# SHIP_VERIFICATION_MATRIX — 23-Item Audit Crosswalk

| Field | Value |
|---|---|
| Generated | 2026-05-09 |
| Source-of-truth | `/Users/nitishbhardwaj/Downloads/index-20-validated.md` (TOC §§1.1-1.11, §§2.1.1-2.3.1, §3 Drops, §§5.1-5.4 — line ranges per row below) |
| Branch | `fix/critical-broadcast-reliability-2026-04-21` |
| HEAD when matrix authored | `d97b59079f62e87f6662ba2d367a548c7f3ff2d1` (Phase 3 — `git show HEAD:` resolves to this SHA) |
| Phase commit chain | `c3808abf` (Phase 1: outbox-hardening) → `0baa7955` (Phase 2: timer-replay) → `d97b5907` (Phase 3: cutover-cleanup) |
| Companion docs | `runbooks/SKIP_DECISIONS.md` · `runbooks/operator-ff-flips.md` · `runbooks/worker-ramp-step1.md` · `runbooks/worker-ramp-step2-3.md` · `runbooks/reaper-leader-verify.md` · `runbooks/m-007-outbox-leader-fencing-sql.md` |

## Status legend

| Marker | Meaning |
|---|---|
| ✅ **committed** | Code lives in this session's phase chain (`c3808abf`/`0baa7955`/`d97b5907`) or in the pre-session bundle commit `ce847ae3`; verified by `git show <SHA>:<file>` lookups at the lines listed |
| 📋 **operator-pending** | Code shipped but the production-flip / soak / ramp action is still operator-driven; runbook column points to the canonical document |
| ⚪ **skip-forever** | Drop confirmed by Round-6 + Round-7 + (where applicable) Round-8 retraction; see `runbooks/SKIP_DECISIONS.md` |

---

## §1 — 11 NEEDED Fixes (✅ VALIDATED)

| ID | Title | Spec § | Spec lines | Implementation file:line(s) at HEAD `d97b5907` | Commit SHA | Test file | Runbook | Status |
|---|---|---|---|---|---|---|---|---|
| **#1** | Timer scan LIMIT + poll interval configurable | §1.1 | 164-278 | `src/shared/services/redis.service.ts:2726-2807` (ARGV-typed Lua LIMIT in `getExpiredTimers`); `src/modules/order/order-timer.service.ts:45-46, 197-212` (env-driven `ORDER_TIMER_BATCH_LIMIT` + per-pod jitter via `startOrderTimerChecker`); `.env.example` `TIMER_BATCH_LIMIT=100` / `TIMER_BATCH_LIMIT_MAX=500` | `0baa7955` (Phase 2) | `src/__tests__/fix-1-timer-limit-configurable.test.ts` | — | ✅ committed |
| **#2** | Raise `REDIS_QUEUE_WORKERS=1→16` (gradual ramp) | §1.2 | 279-372 | `.env.production:65` already `REDIS_QUEUE_WORKERS=16`; `src/shared/database/prisma.service.ts:295-305` URL guard for `connection_limit`/`pool_timeout`; ECS task-def env update + RDS class change (no source code commit) | pre-session (`.env.production`) — operator action drives ECS task-def revision + RDS upgrade | `src/__tests__/fix-prisma-service-hardening.test.ts` (URL guard regression coverage) | `runbooks/worker-ramp-step1.md` · `runbooks/worker-ramp-step2-3.md` · `runbooks/worker-ramp-soak-dashboard.md` · `runbooks/rds-upgrade.md` · `runbooks/ecs-db-connection-limit.md` · `runbooks/hpa-cap.md` | 📋 operator-pending |
| **#16** | BLMOVE reaper cluster-wide leader lock | §1.3 | 373-512 | `src/shared/services/queue.service.ts` reaper body — `acquireLock('processing-reaper:${queue}', workerId, 10)` per-queue gate; `REAPER_PROCESSING_CAP=500` (was 10_000) for TTL fit; `src/shared/config/feature-flags.ts` `PROCESSING_REAPER_LEADER_LOCK` (env `FF_PROCESSING_REAPER_LEADER_LOCK`, default ON) | `ce847ae3` (pre-session — Wk0 0.1 bundle) | `src/__tests__/scenarios/phase4-blmove-reaper.test.ts` (88 LOC) · `src/__tests__/scenarios/phase4-blmove-primitives.test.ts` | `runbooks/reaper-leader-verify.md` · `runbooks/operator-ff-flips.md` (Fix #16 section — flag default-ON, no operator flip required, but verify runbook) | 📋 operator-pending (verify only — no flip) |
| **#17** | Cross-pod room replay + auto-join SADD writes | §1.4 | 513-651 | `src/shared/services/socket.service.ts:97-130` (`trackRoomMembership(roomKey, userId)` helper, 24h TTL) + 16 `socket.join` sites at `:449, :451, :458, :461, :465, :469, :496, :500, :515, :528, :555, :623, :667, :1086…` + 3 emit-function cross-pod branches (`emitToRoom`, `emitToTransporterDrivers`, `emitToAllTransporters`) | `0baa7955` (Phase 2 — SADD writes only; FF flip deferred per §4.2) | `src/__tests__/fix-17-sadd-writes.test.ts` | `runbooks/operator-ff-flips.md` (Fix #17 section — FF_CROSS_POD_ROOM_REPLAY flip after ≥24h SADD bake / 48h conservative ceiling) | 📋 operator-pending (FF flip) |
| **#20** | Outbox leader poller fail-CLOSED on Redis error | §1.5 | 653-787 | `src/modules/order/order-dispatch-outbox.service.ts:532-545` (fenced catch — fail-CLOSED `return`, `outbox_leader_election_redis_error_total{path="fenced"}`); `:559-572` (legacy catch — same fail-CLOSED + `path="legacy"`); B-N-13 canonicalization at `:540` (`acquireLeader` on raw `outbox:leader`); metric registered at `src/shared/monitoring/metrics-definitions.ts:128-130` | `c3808abf` (Phase 1) | `src/__tests__/fix-20-fail-closed.test.ts` · `src/__tests__/fix-bn13-outbox-canonical-key.test.ts` · `src/__tests__/fix-outbox-leader-fencing.test.ts` | `runbooks/m-007-outbox-leader-fencing-sql.md` (M-007 SQL + flag flip sequence per §4.1) | 📋 operator-pending (M-007 SQL + FF_OUTBOX_LEADER_FENCING flip) |
| **#22** | India-bounds tidy at order-CREATE (5-LOC test cleanup; main fix REDUNDANT) | §1.6 / §4.5 | 789-814, 2238-2242 | `src/__tests__/low-priority-final.test.ts` (32-LOC drifted closure DELETED — production `coordinatesSchema.refine`/`locationSchema.refine` at `validation.utils.ts:62-100` is the canonical gate); coverage preserved in `qa-validation-scenarios.test.ts:46-100` | `ce847ae3` (pre-session — bundled with Fix #6/#16/#19c/NEW#4 tidy) | `src/__tests__/qa-validation-scenarios.test.ts` (canonical) · `src/__tests__/validation-fixes.test.ts` · `src/__tests__/falcon-fi1-validation.test.ts` | — (test-only delta, no production blast radius per §4.5) | ✅ committed |
| **#23** | Skip emit when `pickupData` missing (no "0 km / 0 min") | §1.7 | 816-912 | `src/modules/order/order-broadcast.service.ts:996-1004` (`continue;` after warn inside `if (!pickupData)` guard); `:1006` (`alreadyNotifiedSet.add` MOVED below guard so skipped transporters retry next progressive step); mirror at `src/modules/order/order-broadcast-send.service.ts:675-683` (same `continue;` pattern) | `d97b5907` (Phase 3) | `src/__tests__/fix-23-pickup-data-guard.test.ts` (7 tests) | — | ✅ committed |
| **#31** | Atomic Lua `INCR+EXPIRE` (replaces non-atomic pair) | §1.8 | 914-1044 | LIVE bug fix at `src/modules/order/order.service.ts:616-628` (canonical — NOT deprecated `src/modules/booking/order.service.ts`) via `redisService.incrementWithTTLAndRemaining(BACKPRESSURE_KEY, BACKPRESSURE_TTL_SEC)`; sweep across 14 sites (auto-redispatch, booking-create, booking, driver-auth, reassign-driver, transporter-rate-limit, cascade-dispatch, pod, tracking, broadcast routes — see commit body); helper at `src/shared/services/redis.service.ts:2474-2525` | `0baa7955` (Phase 2) | `src/__tests__/fix-31-atomic-incr-expire.test.ts` | — | ✅ committed |
| **#36** | Timer ZSET silent eviction → log + counter + DLQ | §1.9 | 1046-1212 | `src/shared/services/redis.service.ts:2632-2720` (per-prefix DLQ ZSET `dlq:timers:evicted:{prefix}` keyed by timerKey, score=expiresAt; 7-day EXPIRE applied INSIDE same atomic Lua via `TIMER_DLQ_EXPIRE_SECONDS=604800`); helper `timerDlqZset()` at `:1871-1872`; counter `timer_evicted_to_dlq_total{prefix}` at `:2714`; new drainer `scripts/replay-timer-evictions.ts` (378 LOC, leader-elected, ZCARD depth probe + LIMIT-aware Lua fetch) | `0baa7955` (Phase 2) | `src/__tests__/fix-36-timer-dlq.test.ts` | — | ✅ committed |
| **#37** | Outbox tuning — POLL_MS / BATCH / parallelism / TTL | §1.10 | 1214-1322 | `src/modules/order/order-dispatch-outbox.service.ts:482-491` (`OUTBOX_LEADER_TTL_SECONDS=120` was 60; heartbeat=40000ms was 20000ms — re-enabled this commit because B-N-13 canonical key landed in same PR); `package.json` adds `p-limit@^3.1.0` (CommonJS-compatible; v4+ ESM-only avoided) — added in pre-session `ce847ae3` | `c3808abf` (Phase 1 — TTL/heartbeat); `ce847ae3` (pre-session — `p-limit` dep) | `src/__tests__/fix-37-outbox-tuning.test.ts` | — | ✅ committed |
| **#44** | Cancel-path in-TX `VehicleTransitionOutbox` INSERT | §1.11 / §4.3 | 1324-1429, 2206-2215 | `src/modules/order/order-cancel.service.ts:218` (`releasedVehicleData` lifted scope); `:487-494` (population block inside TX after `tx.vehicle.updateMany`); `:497-514` (Fix #44 in-TX `tx.$executeRaw INSERT INTO "VehicleTransitionOutbox"` gated by `isEnabled(FLAGS.VEHICLE_TRANSITION_OUTBOX)`); `:683-692` (legacy fire-and-forget guarded behind `!isEnabled(FLAGS.VEHICLE_TRANSITION_OUTBOX)`) | `c3808abf` (Phase 1) | `src/__tests__/fix-44-cancel-outbox-insert.test.ts` · `src/__tests__/fix-vehicle-transition-outbox.test.ts` | `runbooks/operator-ff-flips.md` (FF_VEHICLE_TRANSITION_OUTBOX flip after table verify per §4.3) | 📋 operator-pending (FF flip after `\d "VehicleTransitionOutbox"` verify) |

---

## §2 — Conditional / Partial / Low-Priority Fixes (4 actionable)

| ID | Title | Spec § | Spec lines | Implementation file:line(s) at HEAD `d97b5907` | Commit SHA | Test file | Runbook | Status |
|---|---|---|---|---|---|---|---|---|
| **#6** | Batch queue depth guard (env-flip + drainer pre-flight) | §2.1.1 / §4.6 | 1437-1521, 2244-2254 | `src/shared/services/queue.service.ts:163-166` (`BROADCAST_QUEUE_DEPTH_CAP=5000`, env-tunable); `:172` (`FF_BATCH_QUEUE_DEPTH_GUARD` reader); `:2367-2434` (overflow → `dlq:broadcasts` with `lTrim(0, DLQ_MAX_SIZE-1)`); `src/server.ts:1088-1106` (`FF_DLQ_DRAINER_ENABLED !== 'false'` registers 30s leader-elected drainer); `scripts/replay-broadcast-dlq.ts` (drainer); 30s sidecar emitter for `dlq_broadcasts_depth` gauge | `ce847ae3` (pre-session — Wk0 0.1 bundle) | `src/__tests__/scenarios/queue-backpressure.test.ts` (274 LOC) · `src/__tests__/durable-emit-contract.test.ts` (T1-T7 DLQ semantics) | `runbooks/operator-ff-flips.md` (Fix #6 section — `FF_BATCH_QUEUE_DEPTH_GUARD=true` flip after pre-flight) · `runbooks/cloudwatch-dlq-alarms.md` · `runbooks/dlq-sidecar-verify.md` · `runbooks/dlq-drainer-preflight.md` · `runbooks/ff-depth-guard-flip.md` (deprecated → operator-ff-flips.md) | 📋 operator-pending (env flip after pre-flight) |
| **#19c** | `lookup_error` silent-drop → DLQ carve-out | §2.1.3 | 1599-1746 | `src/shared/services/queue.service.ts:1547-1577` (catch block — `dlq:broadcasts` push with `reason: 'guard_lookup_error'`, `attempt: 1`); `:1638-1642` (Phase 3 — `dlq_pushed_total{queue='broadcast_guard_lookup_error'}` success-path counter mirrors canonical pattern at `:392 + :2369`); single-Lua `LPUSH+LTRIM` atomic at `:1646`; drainer-side `MAX_REPLAY_ATTEMPTS=5` semantic in `scripts/replay-broadcast-dlq.ts` | `c3808abf` (Phase 1 — DLQ push); `d97b5907` (Phase 3 — `dlq_pushed_total` counter) | `src/__tests__/fix-19c-dlq-push.test.ts` · `src/__tests__/fix-19c-drainer-semantic.test.ts` (10 tests) · `src/__tests__/durable-emit-contract.test.ts` (T1-T7) | — | ✅ committed |
| **#21** | Step-timer claim atomicity — `setTimerIfAbsent` | §2.1.4 | 1748-1880 | `src/shared/services/redis.service.ts:2848-2945` (NEW 2-key Lua helper: `SET-NX` + `ZADD` to shard ZSET + cap-and-evict + EXPIRE-if-`-1`); `src/modules/order/order-broadcast.service.ts:1213-1228` (TOCTOU eliminated — `redisService.setTimerIfAbsent(...).catch(() => false)` replaces `hasTimer`+`setTimer` race); `cancelTimer` ZREM updated to dual-target shard ZSET + legacy `timers:pending` while `FF_TIMER_LEGACY_ZSET_ENABLED` | `0baa7955` (Phase 2 — bundled with #1/#36/NEW#1/NEW#2 timer overhaul) | `src/__tests__/fix-21-set-timer-if-absent.test.ts` (250 LOC) | — | ✅ committed |
| **#13** | Delete dead `acceptBroadcast` (568 LOC actual, was claimed 312) | §2.2.1 | 1882-1965 | `src/modules/broadcast/broadcast.service.ts` — 568 LOC removed (lines 388-900 dead method 513 LOC + 55 LOC orphan scaffolding `AcceptBroadcastParams`/`AcceptBroadcastResult` + private accept-metric helpers); 16 test call-sites retargeted to `acceptBroadcast` from `broadcast-accept.service.ts` across `booking-broadcast-lifecycle.test.ts` (7), `critical-fixes-1-4.test.ts` (4), `booking-fixes.test.ts` (5) | `d97b5907` (Phase 3) | `src/__tests__/fix-13-dead-code-deletion.test.ts` (15 tests) · retargeted: `booking-broadcast-lifecycle.test.ts` · `critical-fixes-1-4.test.ts` · `booking-fixes.test.ts` | — | ✅ committed |
| **#12** | `acquireLock` idempotent prefix-strip (4 functions) | §2.3.1 | 1967-2117 | `src/shared/services/redis.service.ts:2886-2888` (`acquireLock` — `lockKey.startsWith('lock:') ? lockKey.slice(5) : lockKey`); `:2963-2965` (`releaseLock`); `:3003-3005` (`isLockHeldBy`); `:3013-3015` (`getLockHolder`) — Phase 0 idempotent shim per §2.3.1 Pillar 2 (Phase 1 caller-normalize + Phase 2 sweeper deferred to "Whenever") | `c3808abf` (Phase 1) | `src/__tests__/fix-12-lock-prefix-idempotent.test.ts` (310 LOC) | — | ✅ committed |

---

## §3 — DROPS (⚪ skip-forever)

| ID | Title | Spec § | Spec lines | Implementation file:line(s) at HEAD `d97b5907` | Commit SHA | Test file | Runbook | Status |
|---|---|---|---|---|---|---|---|---|
| **#4** | H3 geo-indexing flag (`FF_H3_INDEX_ENABLED`) — already enabled in prod | §3 Drop #1 / §1.6 caveat | 2122-2141 | `.env.production:144` (`FF_H3_INDEX_ENABLED=true`); `src/shared/config/feature-flags.ts:217-218` (registry — env override only, no `defaultValue`); `src/modules/order/progressive-radius-matcher.ts:137` (H3 path active); `:250-258` (per-candidate `redisService.exists('h3:pos:${id}')` loop — performance penalty at our 1-transporter density) | n/a — no code change | n/a — monitor only via `broadcast_candidate_lookup_ms{algorithm}` histogram | `runbooks/SKIP_DECISIONS.md` Skip #1 (lines 18-49) | ⚪ skip-forever |
| **#25** | `setTimer` past-`expiresAt` guard — defends against impossible scenario | §3 Drop #2 | 2143-2171 | `src/shared/services/redis.service.ts:2617` (`setTimer` signature unchanged — `Math.max(1, …)` TTL clamp at `:2620`); `:2848` (`setTimerIfAbsent` companion — also no past-guard); 15 producers verified to compute `expiresAt = Date.now() + positive_delta` in-process | n/a — no code change | n/a — no test required (impossible scenario) | `runbooks/SKIP_DECISIONS.md` Skip #2 (lines 51-90) | ⚪ skip-forever |
| **#8** | Server-side dedup SADD before emit (`FF_SEQUENCE_DELIVERY_ENABLED`) — flag already ON | §2.1.2 / §4.7 | 1523-1597, 2256-2266 | `.env.production:140` (`FF_SEQUENCE_DELIVERY_ENABLED=true`); 7 production reader files at HEAD: `queue.service.ts:86`, `socket.service.ts:336/:1452`, `broadcast.processor.ts:45`, `booking-radius.service.ts:240/:486`, `booking-broadcast.service.ts:102`, `booking.service.ts:861/:1472/:1634`, `booking-rebroadcast.service.ts:195`; phantom export at `queue.types.ts:85` (zero non-test importers — deletion bundled with §2.2.1 Fix #13 cleanup) | n/a — no code change (FF canonicalization deferred / phantom export deletion already covered by Fix #13) | n/a — covered by sequence-delivery e2e suites | `runbooks/SKIP_DECISIONS.md` Skip #3 (lines 92-133) | ⚪ skip-forever |

---

## §5 — Bugs the Audit Missed (4 actionable, found by validation)

| ID | Title | Spec § | Spec lines | Implementation file:line(s) at HEAD `d97b5907` | Commit SHA | Test file | Runbook | Status |
|---|---|---|---|---|---|---|---|---|
| **NEW#1** | Prefix-shard timer ZSETs (eliminates LIMIT starvation across 7 prefixes) | §5.1 | 2274-2382 | `src/shared/services/redis.service.ts:1871-1872` (`timerDlqZset(prefixTag)`); shard-key helpers `timerShardZset` / `timerShardPrefixTag` / `timerPrefixToShardZset` (top-level helpers added Phase 2); `setTimer` at `:2617` writes to shard ZSET `timers:pending:{prefix}` for 7 prefixes (cluster-safe `{prefix}` hash-tags); dual-write to legacy `timers:pending` while `FF_TIMER_LEGACY_ZSET_ENABLED` (default ON); `getExpiredTimers` ZREM updated to scoped shard at `:2726-2807`; `cancelTimer` dual-ZREM (shard + legacy) | `0baa7955` (Phase 2 — coupled with NEW#2 per §5.1 hazard 6) | `src/__tests__/new1-new2-timer-shard-isolation.test.ts` (255 LOC) | — | ✅ committed |
| **NEW#2** | `recoverOrphanedStepTimers` interval scheduler + 7-prefix coverage | §5.2 | 2383-2486 | `src/modules/order/order-timer.service.ts:300-315` (`ALL_TIMER_PREFIXES` — 7 entries: `ORDER_STEP_TIMER_PREFIX`, `ORDER_EXPIRY_TIMER_PREFIX`, `timer:assignment-timeout:`, `timer:booking-order:`, `timer:booking:`, `timer:radius:`, `timer:rating-reminder:`); `:317-340` (`recoverOrphanedStepTimers` body — leader-elected via `acquireLock('timer:orphan-recovery', ..., 600)` with heartbeat between prefixes); `:343-358` (`startOrphanRecoveryScheduler` setInterval, `TIMER_ORPHAN_RECOVERY_INTERVAL_MS=300000`); `src/server.ts:1054` boots scheduler | `0baa7955` (Phase 2 — coupled with NEW#1) | `src/__tests__/new1-new2-timer-shard-isolation.test.ts` (covers shard + scheduler) | — | ✅ committed |
| **NEW#3** | FCM phantom-key surgery (`fcm_token:` → canonical `fcm:tokens:{userId}` SREM) | §5.3 | 2487-2611 | `src/shared/services/queue.service.ts:1828-1944` (LIVE inline FCM_BATCH consumer rewritten — accepts BOTH legacy `{tokens: string[]}` AND Round-6-widened `{recipients: Array<{userId,token}>}`; `cleanupDeadToken(token)` helper at `:1877-1893` calls `fcmService.removeToken(userId, token)` → SREM `fcm:tokens:{userId}` per `fcm.service.ts:132/:662`); 2 phantom `redisService.del('fcm_token:${token}')` writes REMOVED at `:1420, :1456`; `DEAD_TOKEN_CODES = {registration-token-not-registered, invalid-registration-token}` at `:1872-1875` (NOT widened to `invalid-argument` per foot-gun guard); producer at `queue.service.ts:2590-2629` INTENTIONALLY UNTOUCHED (sequential rollout per §5.3 lines 2588-2592 — PR-2 flip pending) | `d97b5907` (Phase 3 — consumer; producer flip is separate PR-2) | `src/__tests__/new3-fcm-phantom-key.test.ts` (12 tests) | — | ✅ committed (consumer); 📋 operator-pending (PR-2 producer flip after `bull:fcm_batch:wait LLEN=0`) |
| **NEW#4** | Delete drifted `isInIndiaBounds` test closure (production `coordinatesSchema` is canonical) | §5.4 | 2613-2660 | `src/__tests__/low-priority-final.test.ts` — 32-LOC drifted closure (lat≥8.0/37.0 — TIGHTER than production lat≥6.5/37.0) DELETED; comprehensive coverage retained in `qa-validation-scenarios.test.ts:46-100` (4 corners + overflow); production gate is `coordinatesSchema.refine` / `locationSchema.refine` at `validation.utils.ts:62-100` | `ce847ae3` (pre-session — bundled with #6/#16/#19c/#22 tidies) | `src/__tests__/qa-validation-scenarios.test.ts` (canonical 9+ assertions) · `src/__tests__/qa-integration-scenarios.test.ts:328-343` (6 additional bounds assertions) | — (test-only delta, no production blast radius) | ✅ committed |

---

## Summary stats

| Status | Count | Items |
|---:|---:|---|
| ✅ committed | **15** | #1, #13, #19c, #21, #23, #31, #36, #37, #12, #22, NEW#1, NEW#2, NEW#3 (consumer), NEW#4, plus #44 code |
| 📋 operator-pending | **5** | #2 (worker ramp + RDS upgrade + DB_CONNECTION_LIMIT), #6 (FF flip), #16 (verify only), #17 (FF flip after SADD bake), #20 (M-007 SQL + FF flip), #44 (FF flip after table verify), NEW#3 PR-2 (producer flip after legacy-job drain) |
| ⚪ skip-forever | **3** | #4 (H3 — already on), #25 (setTimer guard — impossible), #8 (SADD — flag already on) |
| **Total** | **23** | 20 fixes + 3 skips |

> Note: `#44` and `#20` appear in both "committed" (code in `c3808abf`) and "operator-pending" (FF flip / SQL run). Counting them by their **next blocking action**: 5 distinct items still need operator hands at the keyboard (`#2`, `#6`, `#17`, `#20`, `#44`, `NEW#3` PR-2; `#16` is verify-only). Total fully-shipped (no operator action remaining): **15 of 20 fixes + 4 NEW**. Total awaiting operator: **5-6 of 20 fixes + 1 NEW#3 follow-up**.

## Cross-reference quick-jump

- **Phase 1 commit `c3808abf`** (outbox-hardening): #20, B-N-13 (bundled with #20), #37 (TTL/heartbeat re-enable), #44, #12 — plus M-007 SQL runbook authored
- **Phase 2 commit `0baa7955`** (timer-replay): #1, #36, NEW#1, NEW#2, #21, #17 (SADD writes only — flag flip Phase 3+), #31
- **Phase 3 commit `d97b5907`** (cutover-cleanup): #23, #19c (`dlq_pushed_total` counter), NEW#3 (consumer), #13 (568-LOC delete + 16 test retarget), `runbooks/SKIP_DECISIONS.md` formalized, `runbooks/SHIP_READY_CHECKLIST.md` authored, `runbooks/PHASE3_FRONTEND_IMPACT.md` authored, `runbooks/operator-ff-flips.md` edited (4 places — Fix #17 SADD-bake duration corrected)
- **Pre-session commit `ce847ae3`** (Wk0 0.1 bundle): #6 (depth-guard backpressure + drainer), #16 (BLMOVE reaper leader lock), #19c sidecar (`dlq_broadcasts_depth` gauge), #37 prereq (`p-limit@^3.1.0` dependency), #22 + NEW#4 tidy (drifted test closure delete)
