# Fixes Applied — Critical (2026-04-21 review)

| Date | Finding | Verdict | Wave | Commit | Owner | Outcome |
|------|---------|---------|------|--------|-------|---------|
| 2026-04-22 | A10-009 | TRUE | W1 | n/a | db-migration-owner | M-015 + M-016 files audited on branch (2040 B / 1974 B; headers FILE-ONLY; idempotent IF NOT EXISTS + DO $$ EXCEPTION); prod psql-apply deferred to ops handoff (DATABASE_URL VPN-gated, unavailable in session) |
| 2026-04-22 | A10-001 | TRUE | W1 | 2b1ee4f8 | db-migration-owner | M-017 SQL file added (CREATE INDEX CONCURRENTLY IF NOT EXISTS truck_hold_ledger_active_find_idx ON TruckHoldLedger(transporterId, orderId, phase) partial); prod psql-apply deferred to ops handoff |
| 2026-04-22 | A10-001 | TRUE | W1 | 14961bcc | ilike-tightening-owner | mode:'insensitive' dropped from findActiveLedgerHold in 2 files (truck-hold-create.service.ts + truck-hold.service.ts); pre-flight data-sanity + EXPLAIN ANALYZE deferred to ops (DATABASE_URL VPN-gated); normalizeVehiclePart write-side lowercasing confirms correctness |
| 2026-04-22 | A10-004 | TRUE | W1 | bd7eabff | prisma-wrapper-owner | withDbTimeout options type now carries maxWait?: number and forwards maxWait ?? 5000 to $transaction (lifts Prisma 2 s library default); retry backoff gains +floor(random*50) ms jitter (Arch 1A — smooths 833 tx/s thundering herd); additive change, zero new tsc errors, caller tests green |
| 2026-04-22 | A10-002 | TRUE | W1 | 859a6fd1 | prisma-wrapper-owner | prisma.service.ts A10-002 comment above DB_POOL_CONFIG pins DB_CONNECTION_LIMIT=125 as the production MUST value (Little's Law: 833 tx/s × 0.6 s P99 = 500 in-flight; 4 pods × 125 = 500 of 1,600 on r6g.large); runtime default stays 25 for dev/t4g.micro. .env half bundled in 229cc49a per coord-note. |
| 2026-04-22 | A13-001 | TRUE | W1 | 229cc49a (.env) + 859a6fd1 (prisma.service.ts) | prisma-wrapper-owner | .env.production.example rewritten with A13-001 header (do-NOT-on-t4g.micro warning) + connection_limit=125&pool_timeout=8 in DATABASE_URL + DB_CONNECTION_LIMIT=125 + DB_POOL_TIMEOUT=8 (landed in 229cc49a via non-targeted git add); prisma.service.ts sizing comment landed in 859a6fd1. See coord-note in 859a6fd1 commit body. |
| 2026-04-22 | A10-005 | TRUE | W1 | 560c221a (confirmed-hold accept+decline) + 229cc49a (flex-hold transitionToConfirmed) | hot-path-tx-owner | 3 bare prismaClient.$transaction hot-path calls wrapped in withDbTimeout: driver_accept_cas + driver_decline_cas (timeoutMs 15_000, maxWait 5_000) in confirmed-hold.service.ts; flex_transition_to_confirmed (Serializable, timeoutMs 8_000, maxWait 5_000) in flex-hold.service.ts. All three now benefit from A10-004 maxWait + Arch 1A jitter retry loop. |
| 2026-04-22 | A02-001 | PARTIAL | W1 | 229cc49a | hot-path-tx-owner | transitionToConfirmed hardened with SELECT …FOR UPDATE row-lock + Serializable + guardedConfirmFlexToConfirmed CAS (phase='FLEX' AND status='active' predicate). Defensive wrapping; method has zero non-test callers today per master-file A02-001 — LOW regression risk. All prior guards (hold-not-found, ownership, phase) + AB3 broadcast-remaining cap preserved. |
| 2026-04-22 | A03-005 | TRUE | W2 | 58459176 | socket-key-owner | trip_assigned fanout in confirmed-hold.service.ts now carries farePerTruck sourced from TruckRequest.pricePerTruck (0 fallback when null). Three additive edits landed inside the W2 socket block only (~254-272 select adds `truckRequest: { select: { pricePerTruck: true } }`; ~402-414 txResult type adds `truckRequest: { pricePerTruck: number } \| null`; ~498-511 per-driver loop destructures `const farePerTruck = fullData.truckRequest?.pricePerTruck ?? 0` and exposes it in driverNotification). FCM block 533-570 deliberately UNCHANGED — fcm-producer-owner will mirror the same key into fcmData under W2-T08. Commit also swept fcm-builder-owner's in-flight A03-001 one-liner (trip_assigned TTL = HOLD_CONFIG.driverAcceptTimeoutSeconds) into HEAD via a race with git commit default-all-staged behavior; commit body mentions only A03-005 — see coord-note below. |
| 2026-04-22 | A13-013 | TRUE | W2 | 7990ea77 | socket-key-owner | trip_assigned socket payload in confirmed-hold.service.ts exposes pickup/drop with both coordinate key conventions. `socketPickup` and `socketDrop` are built once per flush (spread of pickup/drop + `lat: …?.latitude ?? …?.lat ?? 0, lng: …?.longitude ?? …?.lng ?? 0`) and substituted into driverNotification in place of the raw pickup/drop references. FCM data block (~544-555) left untouched — it already resolved `pickup?.latitude ?? pickup?.lat` correctly. Commit scoped with `git commit -- <path>` after Wave-1 race lesson (see A03-005 row). |
| 2026-04-22 | A03-001 | TRUE | W2 (swept via W2-T10 commit) | 58459176 | fcm-builder-owner (work) / socket-key-owner (carrier) | `trip_assigned: 600` → `trip_assigned: HOLD_CONFIG.driverAcceptTimeoutSeconds` in fcm.service.ts (import added). fcm-builder-owner's local edit was staged before socket-key-owner's `git commit` for W2-T10; default all-staged behavior swept the fcm.service.ts hunk into HEAD under commit 58459176. No drift vs. W2-T01 spec; fcm-builder-owner can mark W2-T01 done. Lesson for Wave 2: always use `git commit -- <pathspec>` (repeated here for Wave-1 reinforcement). |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T03 | fcm-producer-owner | broadcast-accept.service.ts FCM trip_assigned payloads now include nested pickup/drop structured object + `payload: JSON.stringify(obj)` blob + stringified pickup/drop flat fallback. Drift-defense grep found 2 trip_assigned FCM call-sites (retry path ~465 and queue-fallback path ~479); both patched via shared fcmDataBase. Pre-accept DPDP (A09-001) applied: customerName intentionally omitted from FCM data. farePerTruck/distanceKm stringified per FCM data constraints. Import added: maskPhoneForExternal from shared/utils/pii.utils. Socket driverNotification left untouched (W3 scope). tsc clean on canonical file. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T04 | fcm-producer-owner | assignment-dispatch.service.ts FCM trip_assigned payload (queuePushNotification ~222-234) now includes nested pickup/drop + `payload` blob + stringified pickup/drop flat fallback. Drift-defense grep confirmed 1 trip_assigned FCM call-site (line 226); patched. Pre-accept producer — customerName omitted (A09-001). booking.pickup / booking.drop already loaded in scope (line 35); HOLD_CONFIG + maskPhoneForExternal imports added. tsc clean. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T05 | fcm-producer-owner | truck-hold.service.ts FCM block (~1647-1678; disjoint from W1's 594-595 edit) now emits nested pickup/drop + `payload` blob. Drift-defense grep confirmed 3 trip_assigned sites (2 socket assignmentsData/driverNotification at 1587/1613, 1 FCM at 1648) — FCM block ONLY patched per task scope; socket blocks left untouched (W3 scope). Pre-accept: customerName removed from FCM data (A09-001). Preserved legacy flat pickupLat/pickupLng/dropLat/dropLng + added nested `pickup`/`drop` JSON strings + canonical `payload` blob. `fare` legacy alias preserved alongside new `farePerTruck`. tsc clean. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T06 | fcm-producer-owner | cascade-dispatch.service.ts FCM block (~431-458) now emits nested pickup/drop + `payload` blob + stringified pickup/drop. Drift-defense grep confirmed 2 trip_assigned sites (1 socket at 403, 1 FCM at 444); FCM block patched only. Pre-accept producer — customerName omitted (A09-001). Extended order select to include distanceKm + customerPhone; added separate truckRequest select for pricePerTruck → farePerTruck. `isCascade: true` preserved (stringified in FCM data per FCM constraints). maskPhoneForExternal import added. tsc clean. |
| 2026-04-22 | A03-002 | TRUE | W2 | d2832c31 | team-lead (fcm-builder-owner stalled) | Flag-gated data-only FCM payload for FULLSCREEN_TYPES. New release flag FCM_DATA_ONLY_FULLSCREEN (env FF_FCM_DATA_ONLY_FULLSCREEN, defaultValue: false) registered in feature-flags.ts:310-315. fcm.service.ts buildMessage computes `dataOnlyFullscreen = isFullScreen && isEnabled(FLAGS.FCM_DATA_ONLY_FULLSCREEN)`; when true, omits top-level `notification:` AND `android.notification:` (so Android Doze delivers via onMessageReceived → Captain BroadcastFullScreenNotifier.notify launches overlay). Title/body mirrored into `data` bag for client rendering. APNs path unchanged. Default OFF = no behaviour change until ops flips after Captain bbc22c9+ at ≥90% DAU. Lead-executed after fcm-builder-owner went silent across 2 cron cycles. |
| 2026-04-22 | A05-001 | TRUE | W2 | d2832c31 | team-lead (fcm-builder-owner stalled) | Same buildMessage conditional-strip as A03-002 (same commit — synthesis landing per master-file guidance that A03-002 and A05-001 share root cause). `FULLSCREEN_TYPES.has(type) && isEnabled(FLAGS.FCM_DATA_ONLY_FULLSCREEN)` now strips `notification:` block that was unconditional before. iOS APNs preserved unchanged. |
| 2026-04-22 | A13-004 | TRUE | W2 | d2832c31 | team-lead (fcm-builder-owner stalled) | Synthesis row — A13-004 restates A03-002 + A05-001 at scale impact level. Same flag-gated buildMessage fix (d2832c31) resolves all three. Captain's WeeloFirebaseService.kt:92-104 FULLSCREEN_TYPES comment "must wake overlay pipeline from Doze" is now satisfiable when FF_FCM_DATA_ONLY_FULLSCREEN flips ON. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T07 | fcm-producer-owner | reassign-driver.service.ts FCM block for NEW driver (~403-418) now emits nested pickup/drop + `payload` blob. Drift-defense grep confirmed 1 trip_assigned FCM site (line 408) + 1 assignment_update site (line 386 for old driver — out of scope). Added targeted DB fetch: order→pickup/drop/distanceKm/customerPhone + truckRequest→pricePerTruck (with booking fallback when no orderId). Non-fatal try/catch around load; defaults zero/empty if load fails. Pre-accept DPDP (A09-001): customerName omitted. `isReassigned: true` preserved (stringified). maskPhoneForExternal import added. tsc clean. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T09 | fcm-producer-owner | order-accept.service.ts FCM data block ONLY (~549-565) now emits nested pickup/drop + `payload` blob. Drift-defense grep confirmed 2 trip_assigned sites: socket driverNotification at ~527 (W3 scope — NOT touched; customerName line 537 owned by W3 pii-logs-owner) + 1 FCM at ~553 (in scope). Only the FCM `data` block patched. orderPickup/orderDrop typed as LocationRecord in scope; `latitude`/`longitude` used directly without casts. HOLD_CONFIG + maskPhoneForExternal already imported. orderCustomerPhone already masked upstream (txResult line 375). tsc clean. |
| 2026-04-22 | A05-003 | TRUE | W2 | W2-T08 | fcm-producer-owner | confirmed-hold.service.ts FCM data block (~540-562; disjoint from socket-key-owner's W2-T10/T11 at 254-525 which landed first) now emits nested `pickup`/`drop` JSON + canonical `payload` blob + `farePerTruck` sourced from `farePerTruck = fullData.truckRequest?.pricePerTruck ?? 0` (destructured earlier via socket-key-owner's W2-T10). Legacy flat pickupLat/pickupLng/pickupAddress/pickupCity/dropLat/dropLng/dropAddress/dropCity/distanceKm all preserved per master-file guidance ("latitude ?? lat fallback intact"). customerName preserved as-is (W3 pii-logs-owner scope per plan §7.3.2 note; confirmed-hold lines 508/549 in master-file already addressed per A12-008 evidence). Drift-defense grep confirmed 2 trip_assigned sites (1 socket at line 505 — W3 scope / W2-T11 completed; 1 FCM at line 543 — in scope). Serial commit ordering honoured — socket-key-owner's 58459176 + 7990ea77 landed first; this commit rebases cleanly on top. tsc clean. |
| 2026-04-22 | A03-004 | TRUE | W3 | fc8e86e1 | outbox-fanout-owner | W3-T06 — added `TripAssignedFanoutPayload` interface to `src/modules/order/order-types.ts` mirroring the fast-path driver notification shape (orderId/tripId/assignmentId/driverId/transporterId/bookingId/truckRequestId/pickup/drop canonical {latitude,longitude,...}/farePerTruck:number/distanceKm/vehicleNumber/vehicleType/customerName/customerPhone-already-masked/assignedAt-ISO/expiresAt-ISO/message/eventId/eventVersion/serverTimeMs) and extended `OrderLifecycleOutboxPayload` union to include it. Plan-approval gate honoured (team-lead approved request w3-t06-outbox-fanout-owner). Known temporary tsc gap at `order-lifecycle-outbox.service.ts:668` (bare `else` exhaustion — TS2345) documented in commit body; closes within this session via W3-T07 dispatcher branch. Scoped commit via `git commit -m ... -- src/modules/order/order-types.ts`. |
| 2026-04-22 | A13-005 | TRUE | W3 | fc8e86e1 | outbox-fanout-owner | W3-T06 — synthesis row; A13-005 restates A03-004 at scale-impact level (0.1% ECS restart × 833 holds/s × 3 drivers = ~648K lost notifications/day). Same commit (fc8e86e1) lands the type foundation. Full durability lands via W3-T07 (dispatcher) + W3-T08 (inside-tx producer write, flag-gated FF_TRIP_ASSIGNED_FANOUT_OUTBOX_ENABLED default OFF) + W3-T09 (observability counter). |
| 2026-04-22 | A12-002 | TRUE | W3 | 76ff0566 | pii-logs-owner | order-accept.service.ts:476 `Driver: ${driverName} (${driverPhone})` template-string log replaced with structured `logger.info('Driver notified', { driverId, assignmentId, driverPhoneLast4: maskPhoneForExternal(driverPhone \|\| '') })`. logger.service.ts SENSITIVE_FIELDS extended from 9 entries to 14 by adding phone, name, customerName, driverName, customerPhone, driverPhone — substring-match sanitizer now auto-redacts any meta key containing these tokens. Systemic gap closed (A12-007 compound). tsc clean on both files. maskPhoneForExternal already imported in order-accept.service.ts via W2-T09. |
| 2026-04-22 | A01-004 | TRUE | W3 | 8128d634 | pii-logs-owner (backend half — Arch 1C absorption) | truck-hold.routes.ts:628-647 POST /truck-hold/confirmed-hold/initialize now validates per-item fields before reaching service layer. After the existing length check, a for-loop asserts each assignments[i] has non-empty-string assignmentId, driverId, truckRequestId; on failure returns 400 VALIDATION_ERROR mentioning the index and the three required fields. Prevents Captain's legacy {vehicleId, driverId} payload shape from reaching confirmed-hold.service.ts:152 where `a.assignmentId` (undefined) produced empty IN-clause silent-success. Captain wire-class half (W3-T18/T19) spawned by lead as subagent post green-gate per §7.3.2 Arch 1C. tsc clean. |
| 2026-04-22 | A09-001 | TRUE | W3 | b34c4a77 | pii-logs-owner | broadcast-accept.service.ts:456 driverNotification object literal no longer carries `customerName: booking.customerName`; inline `'X'.repeat(...) + slice(-4)` phone mask replaced with canonical `maskPhoneForExternal(booking?.customerPhone \|\| '')`. Pre-accept customer identity now gated behind post-accept `GET /assignment/{id}/pickup-details`. DPDP Act 2023 S.5(b) data-minimisation closure for the broadcast-accept producer site. FCM data block already DPDP-clean from W2-T03. Line 233 `customerName: string` type declaration untouched (describes the booking DB row). tsc clean. |
| 2026-04-22 | A03-004 | TRUE | W3 | adb24fc6 | outbox-fanout-owner | W3-T07 — src/modules/order/order-lifecycle-outbox.service.ts now parses + dispatches `trip_assigned_fanout` outbox rows. Added: (1) TripAssignedFanoutPayload import, (2) `if (type === 'trip_assigned_fanout') return parseTripAssignedFanoutPayload(raw)` branch in parseLifecycleOutboxPayload (line 90-92), (3) `parseTripAssignedFanoutPayload` function with strict required-field validation (assignmentId/driverId/transporterId/orderId; nullable tripId/bookingId/truckRequestId; coerced pickup/drop canonical {latitude,longitude,...}), (4) resolveOutboxOrderId extended to partition trip_assigned_fanout by orderId, (5) `dispatchTripAssignedFanoutFromOutbox` — replays emitToUser('trip_assigned', driverNotification) with A13-013 lat/lng aliases + queuePushNotification with nested pickup/drop + `payload` JSON blob + flat legacy keys (A05-003 shape); metrics `new_assignment_socket_emit_total{source=outbox_replay}` + `new_assignment_fcm_enqueue_total{source=outbox_replay}`, (6) branch `else if (payload.type === 'trip_assigned_fanout')` in processLifecycleOutboxRow (line 903), (7) errorLabel switch extended with TRIP_ASSIGNED_FANOUT_EMIT_FAILED. Closes the tsc exhaustion gap opened by W3-T06 (bare else at :668 now narrows back to OrderCancelledOutboxPayload). Idempotency on replay: _seq ZSET dedups duplicate socket emits at Captain; FCM collapseKey dedups in tray. Regression: npx jest critical-fixes-dispatch-payment -t parseLifecycleOutboxPayload → 2 passed (legacy trip_completed parse unchanged). tsc clean on canonical file (errors only in `.service 2.ts` junk duplicate pre-existing per baseline). |
| 2026-04-22 | A13-005 | TRUE | W3 | adb24fc6 | outbox-fanout-owner | W3-T07 — synthesis row; A13-005 restates A03-004 at scale-impact level. Same commit (adb24fc6) lands the dispatcher half. Full durability closed by W3-T08 (inside-tx producer write, flag-gated default OFF) + W3-T09 (observability counter `confirmed_hold_fanout_total`). |
| 2026-04-22 | A12-008 | TRUE | W3 | 49d3c2aa | pii-logs-owner | order-accept.service.ts:531-548 socket trip_assigned payload: `customerName: orderCustomerName` line removed; raw `customerPhone: orderCustomerPhone` replaced with `customerPhone: maskPhoneForExternal(orderCustomerPhone \|\| '')`. Pre-accept DPDP posture now aligned with FCM data block (W2-T09 already masked/stripped). orderCustomerName destructuring at line 374 retained (needed for downstream audit/lifecycle writes; never emitted cross-role). Completes the A09-001/A12-008 compound DPDP closure across the four W3 producer sites (broadcast-accept, order-accept; confirmed-hold + cascade-dispatch already addressed per master-file evidence). tsc clean. |
| 2026-04-22 | A04-003 | PARTIAL | W3 | f593e744 | role-scoped-zset-owner | socket.service.ts setupRedisAdapter success branch (line ~1401) now emits logger.warn('[Socket] adapter does NOT reclaim cross-instance PEL on pod death — rely on per-user ZSET durable-emit for lifecycle event replay. See A04-003 (adapter uses plain XREAD, not XREADGROUP).') followed by metrics.incrementCounter('socket_adapter_no_pel_reclaim_total', { adapter: '@socket.io/redis-streams-adapter' }) inside a try/catch (metrics optional at startup). Counter registered in metrics-definitions.ts:421-429 under registerDefaultCounters. PARTIAL verdict preserved per master-file §A04-003 — true XAUTOCLAIM reclaim requires library fork (out of scope); per-user ZSET durable-emit remains the authoritative cross-instance replay path. Pairs one-to-one with adapter init events. Verify: `grep -n socket_adapter_no_pel_reclaim_total src/shared/services/socket.service.ts src/shared/monitoring/metrics-definitions.ts` → 2 hits. tsc clean on both files. |
| 2026-04-22 | A09-002 | TRUE | W3 | 0a12a353 | role-scoped-zset-owner | Role-scoped durable-emit ZSET Phase 1 — ATOMIC MULTI/EXEC dual-write to both `socket:unacked:{userId}` (OLD) and `socket:unacked:{userId}:{role}` (NEW) keys when FF_ROLE_SCOPED_DURABLE_EMIT=on (default OFF). Envelope carries `role` tag. Reader + ACK purger UNCHANGED in Phase 1 (still OLD-key only). On pipeline abort: AppError('DURABLE_EMIT_DUAL_WRITE_FAILED') thrown from durableEmit inner block; outer try/catch degrades to plain emit; counter socket_unacked_dual_write_fail_total{phase:'1'} + logger.error('durable_emit_dual_write_failed',{userId,role,event,errMessage}). Per-success metrics: socket_unacked_key_version{version:'v1'} + {version:'v2'}. Files: src/shared/services/socket.service.ts (durableEmit + persistRoomEnvelopes + userRoleCache + disconnect cleanup + auth cache population), src/shared/services/redis.service.ts (IRedisTransaction.zAdd/incr additive + exec throws on MULTI null / per-op err), src/shared/config/feature-flags.ts (FLAGS.ROLE_SCOPED_DURABLE_EMIT release defaultValue:false), src/shared/monitoring/metrics-definitions.ts (+2 counters). Atomicity test (Arch 1B MUST): src/__tests__/role-scoped-zset-phase1-atomicity.test.ts — 5 cases PASS; 120 baseline durable-emit/socket tests green (zero regression). Perf 4B pre-flight (ops action, not this commit): measure ElastiCache peak IOPS via CloudWatch; assert < 40% of node limit before flipping FF ON on staging; record in wave-3-gate.md. |
| 2026-04-22 | A12-009 | PARTIAL | W3 | 0a12a353 | role-scoped-zset-owner | Consolidated with A09-002 per master-file §A12-009 blocked-by (same root defect — 3-phase rollout shared commit). Phase 1 preserves dual-write for rollback; Phase 2 (W3-T11) flips reader; Phase 3 (W3-T12) removes OLD-key write under 24h staging soak gate. PARTIAL verdict preserved — verifier framed A12-009 as Critical pre-emptive hardening (no active PII leak today under single-role DB schema). |
